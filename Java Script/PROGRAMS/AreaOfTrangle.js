/*If you know the base and height of a triangle, you can find the area using the formula: */
const base = parseFloat(prompt("Enter Base of Trangle :"));
const height = parseFloat(prompt("Enter height of trangle :"));
let area = (base * height)/2;
document.write("Area of Trangle is : "+area);